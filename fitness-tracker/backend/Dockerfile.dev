# 使用官方Node运行时作为父镜像
FROM node:18-alpine

# 设置工作目录
WORKDIR /app

# 复制package.json和package-lock.json（如果存在）
COPY package*.json ./

# 安装所有依赖（包括开发依赖）
RUN npm install

# 复制应用源代码
COPY . .

# 暴露端口
EXPOSE 3001

# 启动开发服务器
CMD ["npm", "run", "dev"]